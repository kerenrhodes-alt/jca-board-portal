# SPA fallback — every path returns index.html so React Router can
# resolve the route on the client. Status 200 makes this a rewrite
# (not a redirect), so the URL stays put when a user hard-refreshes
# or shares a deep link like /documents or /settings.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
